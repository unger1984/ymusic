name: ymusic
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  bloc_concurrency: ^0.2.2
  bitsdojo_window: ^0.1.5
  collection: ^1.17.2
  colorize: ^3.0.0
  english_words: ^4.0.0
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  intl: ^0.18.1
  json_annotation: ^4.8.1
  logging: ^1.2.0
  path: ^1.8.3
  path_provider: ^2.1.1
  system_tray: ^2.0.3
  window_manager: ^0.3.7

dev_dependencies:
  build_runner:
  dart_code_metrics_presets: ^2.6.0
  flutter_lints: ^3.0.1
  freezed: ^2.4.5
  husky: ^0.1.6
  json_serializable: ^6.7.1


flutter:
  uses-material-design: true

  assets:
    - assets/png/
